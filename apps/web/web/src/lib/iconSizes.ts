/** Shared icon size scale. Pick the size that matches context, don't invent
 * one-off pixel values. Weight convention: "regular" for nav/utility icons,
 * "bold" for actionable/CTA icons, "fill" for rating/favorite/active state. */
export const ICON_SIZE = {
  xs: 14,
  sm: 16,
  md: 18,
  lg: 22,
  xl: 28,
} as const;
