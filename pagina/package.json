{
  "name": "localmarket",
  "version": "1.0.0",
  "description": "LocalMarket – scripts raíz que delegan al backend",
  "scripts": {
    "dev":   "cd backend && npm run dev",
    "build": "cd backend && npm run build",
    "start": "cd backend && npm run start",
    "install:backend": "cd backend && npm install",
    "migrate": "cd backend && npm run migrate:win"
  }
}
